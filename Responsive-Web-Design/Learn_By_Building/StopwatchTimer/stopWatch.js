//get elements from our html page i.e using getElementById('idName') and store them in a variable
//set variables to store the minutes, seconds, and millisecond
//the button variables should be bind with addEventListener e.g startButton.addEventListener('click', variableName)
//Create function for each of the button 
//1 sec = 1000ms, 1min = 60sec
//create a function to display new times e.g millisecondslabel = 
